# Nix Environment

This directory contains the Nix configuration for this dotfiles repository.

## Usage

To use the Neovim environment defined in this flake, run the following command:

```bash
nix develop
```

This will drop you into a shell with Neovim and all its plugins available.
